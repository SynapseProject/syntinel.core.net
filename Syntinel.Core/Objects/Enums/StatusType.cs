using System;

namespace Syntinel.Core
{
    public enum StatusType
    {
        New,
        Sent,
        Received,
        SentToResolver,
        Completed,
        Error,
        Invalid
    }
}
